[repository]
name = "hyprfx"
authors = ["sandwich"]

[hyprfx]
description = "GPU-accelerated window open/close effects for Hyprland"
authors = ["sandwich"]
output = "build/libhyprfx.so"
build = [
    "cmake -B build",
    "cmake --build build",
]
